# Wake on LAN Server Configuration

# Server port (default: 8080)
PORT=8080

# JWT Secret for authentication - CHANGE THIS IN PRODUCTION!
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_change_this_in_production

# Allowed origins for CORS (comma-separated)
# Use * for all origins (not recommended for production)
# Examples:
# - Single origin: http://localhost:3000
# - Multiple origins: http://localhost:3000,https://yourdomain.com,https://app.yourdomain.com
# - All origins: *
ALLOWED_ORIGINS=*

# Node.js environment (development, production)
NODE_ENV=production
